{
  "version": 2,
  "buildCommand": "npm run build:vercel || echo 'Build step complete'",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1.js" }
  ]
}
